# 移动端聚会游戏 App · 项目规划（局域网联机版）

> 技术栈：Godot 4.7 stable（GDScript 为主）｜ 项目位置：`D:\Godot Progame\party-games`
> 联网方式：**仅局域网 / 本地热点，无在线服务**
> 目标平台：Android / iOS
> 文档版本：v2.0 ｜ 日期：2026-09-23
> 变更说明：v2.0 由「在线实时多人」改为「局域网联机」，删除了服务器、账号、运营等章节，并重排期与风险清单。

---

## 0. 这个决定带来了什么

**砍掉的：**

| 砍掉的东西 | 省下的成本 |
|---|---|
| 服务器开发与运维 | 约 4~6 周开发 + 每月云成本 |
| 账号系统、实名认证、防沉迷 | 约 2 周 |
| 匹配/大厅服务、排行榜、好友 | 约 3 周 |
| 云端配置下发、热更新 | 约 1 周 |
| ICP / 文网文 / 网络文化经营相关资质 | 法务与时间成本 |

**新增的：**

| 新增的东西 | 说明 |
|---|---|
| 主机权威架构（Host-as-Server） | 房主手机既是玩家也是服务器 |
| 局域网发现 + 扫码配对 | 项目中新的技术模块 |
| 主机退出的容错设计 | 房主掉线 = 房间解散，必须做体验兜底 |
| 局域网排障引导 UI | **实际比重比想象中大**，见第 9 章 |

**排期从约 21 周降到约 16 周。最大风险也从"笔迹同步手感"变成了"局域网到底连不连得上"。**

---

## 1. 项目定位

### 1.1 一句话定位

朋友聚会时掏出一部手机开热点，其他人扫码进房，30 秒内开始玩。

### 1.2 目标场景（按重要性排序）

1. **手机热点直连** —— 房主开热点，其余人连上。**最可靠，应该作为主推路径。**
2. **同一 Wi-Fi 局域网** —— 在家、在宿舍、在咖啡馆。方便，但受路由器设置影响。
3. **单机热座** —— 一台手机传着玩。作为"连不上时也能玩"的兜底。

### 1.3 核心体验指标

| 指标 | 目标 |
|---|---|
| 房主从打开 App 到显示二维码 | < 15 秒 |
| 其他玩家从扫码到进入房间 | < 10 秒 |
| 局域网内单次输入延迟 | < 30 ms |
| **首次尝试连接成功率** | **≥ 95%**（最关键） |

最后一条是局域网游戏的生命线。连不上一次，玩家就不会再打开第二次。

---

## 2. 游戏阵容与优先级

| 优先级 | 游戏 | 人数 | 单局时长 | 核心乐趣 | 局域网下的技术难点 |
|---|---|---|---|---|---|
| **P0** | 你画我猜 | 3~8 | 5~8 min | 画功暴露、爆笑 | 笔迹实时同步（局域网下难度大幅下降） |
| **P0** | 画猜接龙 | 3~8 | 8~12 min | 信息扭曲的荒诞感 | 回合流水线、揭晓回放 |
| **P0** | UNO（派对规则集） | 2~8 | 5~10 min | 翻盘、互害 | 主机权威、手牌单发、主机掉线容错 |
| P1 | 环游中国（大富翁玩法） | 2~6 | 10~15 min | 买地、垄断、翻盘 | 固定轮数压缩时长、竖屏棋盘信息密度（详见 `docs/环游中国（大富翁玩法）规划.md`） |
| P1 | 谁是卧底 | 4~10 | 6~10 min | 语言博弈 | 描述轮次、投票 |
| P1 | 数字炸弹 / 你说我猜 | 3~8 | 3~5 min | 快节奏暖场 | 轻量，可复用框架 |
| P2 | 派对小游戏合集（反应力/记忆/点击） | 2~8 | 1~3 min | 填充空档 | **局域网延迟 < 30ms，可以做真正的实时对战小游戏** |

> 局域网低延迟是这个方案被低估的优势。在线模式下不敢做的"实时反应类"玩法（抢答、抢拍、同步按键）在局域网下完全可行，而且这类玩法在聚会场景里效果极好，值得作为差异化方向。

---

## 3. 联网架构：主机即服务器

### 3.1 基本模型

```
        ┌──────────────────────┐
        │   房主手机（HOST）    │
        │  ┌────────────────┐  │
        │  │ 房间服务器逻辑   │  │  ← 权威：出牌校验、回合调度、计分
        │  └────────────────┘  │
        │  ┌────────────────┐  │
        │  │  玩家界面（P1） │  │
        │  └────────────────┘  │
        └───────┬──────────────┘
     UDP/ENet   │
   ┌────────────┼────────────┬──────────────┐
   │            │            │              │
┌──▼───┐   ┌───▼──┐   ┌─────▼──┐   ┌───────▼─┐
│ P2   │   │ P3   │   │  P4    │   │  P5..P8 │
│客户端 │   │客户端 │   │ 客户端 │   │  客户端  │
└──────┘   └──────┘   └────────┘   └─────────┘
```

Godot 天然支持这个模型：`ENetMultiplayerPeer.create_server()` 和 `create_client()`，权威逻辑用 `@rpc("any_peer", "call_local", "reliable")` 配合 `multiplayer.is_server()` 分支。

### 3.2 为什么 MVP 阶段不做主机迁移

主机迁移（Host Migration）实现成本高（状态序列化、增选新主、重建连接），收益有限：**一局才 5~10 分钟，房主掉线了直接重开一局更省事**。

替代方案：

- **大厅阶段**：房主掉线 → 提示"房主已离开"，弹出"由你成为新房主"按钮 → 新主机重建房间，其他人自动重连。**这个值得做，成本低。**
- **对局阶段**：房主掉线 → 直接结束本局，显示"房主掉线，本局中断" + "重新开局"按钮。**明确告知比静默卡死好一百倍。**

### 3.3 三种配对方式

| 方式 | 可靠性 | 优先级 | 说明 |
|---|---|---|---|
| **二维码扫码** | ★★★★★ | **主推** | 房主显示二维码，内容为 `godotlan://<ip>:<port>`，客户端扫码直连。**绕过广播发现，穿透性最好** |
| **UDP 广播自动发现** | ★★★☆☆ | 辅助 | 客户端进大厅自动扫描局域网房间列表，"点了就进"最省事，但受路由器隔离影响 |
| **手动输入 IP** | ★★★★☆ | 兜底 | 扫码失败时使用。房主界面需同时显示纯文本 IP:端口 |

**强烈建议：二维码是主路径，广播发现是惊喜，手动 IP 是安全网。** 很多团队只做广播发现，遇到客户端隔离就完全废掉。

> 扫码可以直接用相机 + 原生插件，也可以引导用户用系统相机扫码后通过自定义 URL Scheme（`godotlan://`）唤起 App。**后者不需要写原生相机插件，建议优先**，成本低很多。

### 3.4 Godot 实现要点

房主：启动服务器并广播自己

```gdscript
const GAME_PORT := 8910
const DISCOVERY_PORT := 8911

var _udp: PacketPeerUDP
var _max_players := 8

func host_game(room_name: String, max_players := 8) -> Error:
	_max_players = max_players
	var peer := ENetMultiplayerPeer.new()
	# 客户端数 = 总人数 - 1（房主自己也算一个玩家）
	var err := peer.create_server(GAME_PORT, _max_players - 1)
	if err != OK:
		return err
	multiplayer.multiplayer_peer = peer
	_start_discovery_beacon(room_name)
	return OK

func _start_discovery_beacon(room_name: String) -> void:
	_udp = PacketPeerUDP.new()
	_udp.set_broadcast_enabled(true)
	_udp.set_dest_address("255.255.255.255", DISCOVERY_PORT)
	var t := Timer.new()
	t.wait_time = 1.0
	t.timeout.connect(_beacon.bind(room_name))
	add_child(t)
	t.start()

func _beacon(room_name: String) -> void:
	var payload := {
		"n": room_name,
		"p": multiplayer.get_peers().size() + 1,
		"m": _max_players,
		"v": 1,
	}
	_udp.put_packet(JSON.stringify(payload).to_utf8_buffer())
```

客户端：扫描局域网房间

```gdscript
func scan_lan(timeout := 1.5) -> Array:
	var udp := PacketPeerUDP.new()
	if udp.bind(DISCOVERY_PORT) != OK:
		return []
	var found := {}
	var deadline := Time.get_ticks_msec() + int(timeout * 1000.0)
	while Time.get_ticks_msec() < deadline:
		while udp.get_available_packet_count() > 0:
			var text := udp.get_packet().get_string_from_utf8()
			var info = JSON.parse_string(text)
			if info is Dictionary:
				info["ip"] = udp.get_packet_ip()
				found[info["ip"]] = info
		await get_tree().process_frame
	udp.close()
	return found.values()

func join_by_ip(ip: String, port := GAME_PORT) -> Error:
	var peer := ENetMultiplayerPeer.new()
	var err := peer.create_client(ip, port)
	if err != OK:
		return err
	multiplayer.multiplayer_peer = peer
	await multiplayer.connected_to_server     # 记得加超时
	return OK
```

必须注意的几点：

- `create_client()` 返回 OK **不代表连上了**，必须等 `connected_to_server` 信号，**并且一定要加超时（建议 5 秒）**，否则弱网下会无限等待。
- 用 `connection_failed` / `server_disconnected` 信号做错误提示。
- **广播到 255.255.255.255 在部分 Android 机型上会被丢弃**，更稳的做法是同时计算子网广播地址（如 192.168.1.255）也发一份。
- 若使用多播地址（224.x.x.x），Android 需要 `MulticastLock`。纯广播到 255.255.255.255 一般不需要，但各机型差异大，要实测。
- 固定端口容易被占用，建议 8910 失败时依次尝试 8912/8914/8916，**并把实际端口写进二维码**。

### 3.5 平台权限（漏一个就连不上）

| 平台 | 需要做的事 |
|---|---|
| **Android** | `INTERNET` 权限（导出时勾选"网络"）。Android 13+ 读取 Wi-Fi 信息需 `NEARBY_WIFI_DEVICES`；读取 SSID 需 `ACCESS_FINE_LOCATION` |
| **iOS 14+** | **必须在 `Info.plist` 加 `NSLocalNetworkUsageDescription`**，否则系统直接拒绝局域网访问且不报错。首次连接会弹"允许访问本地网络？"，**用户点拒绝就永久连不上，必须引导去设置页重新开启** |
| iOS 若用 Bonjour | 还需 `NSBonjourServices` 声明服务类型 |
| 两端 | 自定义 URL Scheme `godotlan://` 需配置 `Info.plist` / `AndroidManifest.xml` |

> iOS 本地网络权限是这个项目**最容易翻车的点**。它不像相机权限那样有明确的"被拒绝"提示，表现就是"连接超时"，非常难排查。**必须在 M0 阶段用两台真机验证。**

---

## 4. 各游戏的设计调整

### 4.1 你画我猜

局域网延迟 < 30ms，**笔迹同步难度大幅下降**，之前的极端优化可以放宽：

| 项目 | 在线方案 | 局域网方案 |
|---|---|---|
| 传输频率 | 每 50 ms 合批发送 | 可直接每帧或每 20 ms 发送 |
| 点压缩 | 增量 i8 编码 | 建议保留（省电、省内存），但已不是性能瓶颈 |
| 坐标系 | 必须量化统一 | **仍需统一**（机型分辨率不同，这是正确性问题） |
| 顺序保证 | 序列号 + 补发 | 局域网丢包率极低，ENet reliable 通道足够 |

**结论：保留"固定逻辑坐标系 + 量化"设计（这是正确性问题不是性能问题），其余可以简化。**

```gdscript
const BOARD_MAX := 4095

static func to_board(local_pos: Vector2, board_size: Vector2) -> Vector2i:
	var uv := (local_pos / board_size).clampf(0.0, 1.0)
	return Vector2i(
		int(round(uv.x * BOARD_MAX)),
		int(round(uv.y * BOARD_MAX))
	)
```

量化精度 1/4096，在 1080 宽画布上约 0.26 px，肉眼不可辨，且不同机型看到的画完全一致。

#### 玩法与规则

- 房主配置：回合数（默认 3）、每回合时长（默认 80s）、词库难度、提示开关。
- 每回合随机一名玩家当画手；可设置"每人当一次画手"。
- 画手从 3 个候选词中选 1 个，倒计时开始作画。**答案只发给画手，猜词者只收到字数和类别。**
- 有人猜对时提示"XXX 猜对了"，但**不公布答案**，继续到全员猜对或超时。
- 提示机制：超时过半揭示"字数 + 首字"。
- 近似判定：文本规范化（去空格标点、全半角统一）+ 同义词表 + 编辑距离"接近了"提示。

#### 计分

```
猜对者得分 = 100 + 剩余秒数 × 2 × 难度系数
画手得分   = 每个猜对者 50 分 + 全员猜对额外 50 分
```

#### 体验要点

- 画板固定逻辑比例 4:3：上方画板 + 下方笔刷与猜词区，所有玩家几何一致。
- 笔刷精简到 6 项：3 档粗细、橡皮、撤销、清空、9 色调色板，工具栏放在拇指可达区。
- 移动端写字难，**别指望玩家写数字或汉字**，词库要选"能画"的词。
- 长会话下 `Line2D` 节点会持续增长，建议定期把已完成笔迹烘焙进 `SubViewport` 纹理，只保留正在画的 1~2 条为活节点。

#### 词库（局域网版无远端下发）

- 3 档难度 × 每档 300 词 ≈ 900 词，随版本内置（CSV/JSON）。
- 因为没有云端更新，要做两件替代方案：
  - 词库按**分类包**拆分，玩家可开关（如关闭"影视梗"避免有人不懂）；
  - 提供**从本地文件导入自定义词包**（手机存储里的 CSV）。聚会时有人自己加词，反而是不错的社交功能。

### 4.2 画猜接龙

局域网下**几乎无改动**。这是最适合局域网的一款：无实时同步需求，只有回合传递。

#### 玩法循环

```
第 1 步：所有人各写一个题目          → 本子传给下家
第 2 步：下家看题，画出来            → 本子传给下家
第 3 步：下家看画，猜是什么，写下来  → 本子传给下家
第 4 步：下家看文字，画出来          → 本子传给下家
第 5 步：……交替进行，直到本子回到原作者手上
```

#### 关键规则

- **流水线调度**：第 k 步中，玩家 i 处理玩家 `(i - k + N) % N` 的本子。主机维护 `book[step][author]`，每步只下发"上一步的产物"，**绝不提前下发整本**。
- 时长：写词 30s、画画 60s、猜词 30s（可配置），超时自动提交，空白也提交。
- 掉线玩家的格子保留，由主机自动填充，**不能卡住流水线**。
- 揭晓阶段逐本逐页播放，带淡入 + 音效，支持"全部展开"跳过。
- **分享长图**：把某一本的所有页拼成竖版长图（含昵称），一键存相册。纯本地合成，不需要服务器，是很强的传播点。

#### 复用度

画板、笔刷、笔迹序列化、词库全部复用你画我猜，只做 UI 与调度差异。**增量工作量约 1.5 周。**

### 4.3 UNO

#### 牌组（标准 108 张）

| 类型 | 数量 |
|---|---|
| 数字 0（每色 1 张） | 4 |
| 数字 1~9（每色 2 张） | 72 |
| Skip / Reverse / Draw Two（每色 2 张） | 24 |
| Wild（变色） | 4 |
| Wild Draw Four（变色 +4） | 4 |

#### 默认规则（以官方为准）

- 每人发 7 张（2 人局同样是 7 张）。
- 起始翻牌若是 +4，放回牌堆重新翻。
- 无牌可出必须摸 1 张；摸到的牌若可出，可选择立即出。
- 牌堆抽空时，把弃牌堆除最上面一张外洗回。
- 2 人局中 Reverse 视同 Skip。
- 只剩 1 张牌必须喊 UNO；被"抓"到未喊罚摸 2 张。
- 计分：`数字牌 = 面值；Skip/Reverse/Draw2 = 20；Wild/Wild Draw4 = 50`，其他玩家手牌点数之和记给胜者。

#### 房主可配置的家规

| 家规 | 默认 | 说明 |
|---|---|---|
| 叠牌（Stacking） | 关 | +2 可叠 +2；+4 只能叠 +4，不同时混叠 |
| 连续 +4 | 关 | 允许 +4 后接 +4 |
| 7-0 规则 | 关 | 出 7 交换手牌，出 0 全体顺移 |
| 抢出（Jump-in） | 关 | 有完全相同的牌可抢出。**局域网低延迟下体验很好，可以考虑默认开** |
| 摸到即出 | 开 | 摸到可出的牌可立即出 |
| 无牌可出摸到即过 | 开 | 摸完不能出则自动过 |

#### 局域网下的技术设计

1. **主机权威**：出牌合法性、摸牌、技能结算、胜负判定全部在主机执行。
2. **手牌点对点单发**：公共状态（弃牌堆顶、当前颜色、方向、每人剩余张数、当前回合）走广播；手牌用 `rpc_id(peer_id, ...)` 单发。
   - 熟人局作弊风险低，但**这仍是信息正确性问题**：手牌广播给所有人后，一个简单的抓包工具就能看穿全场。保留单发成本很低。
3. **重连**：玩家重连时主机补发该玩家完整手牌 + 公共状态快照。
4. **不采用确定性帧同步**：UNO 有隐藏信息，状态同步更简单可靠。
5. **乐观反馈**：本地立刻播作出牌动画 → 主机确认 → 非法则回滚提示。局域网延迟低，这个体验会很顺。
6. **AI 补位**：掉线超过 30 秒由简单 AI 托管。

---

## 5. 通用框架层

| 模块 | 职责 | 局域网下的简化 |
|---|---|---|
| **本地档案** | 昵称、头像、偏好设置 | 全本地存储，无账号。头像用部件拼接 |
| **房间管理** | 建房、扫码加入、房间列表、玩家注册表、准备状态 | 主机内存态，无需持久化 |
| **游戏状态机** | Idle → 选游戏 → 配置 → 开局 → 回合循环 → 结算 → 回房间 | 不变 |
| **回合调度器** | 当前轮到谁、倒计时、超时行为 | 不变 |
| **计分服务** | 累加、排行、MVP | 不变 |
| **网络层** | ENet 封装、局域网发现、二维码配对、重连、心跳 | **新增的核心模块** |
| **绘制引擎** | 画板、笔刷、撤销、笔迹序列化 | 不变 |
| **商业化** | IAP、广告 | 见第 7 章 |
| **本地埋点** | 事件存本地，可选上报 | 大幅简化 |
| **本地化** | 中/英起步 | 不变 |

### 5.1 游戏统一接口

每款游戏实现同一套契约，房间层只调用这些方法：

```gdscript
class_name MiniGame extends Node

func get_meta_info() -> GameMeta: ...       # 名称、人数范围、时长、图标
func get_config_schema() -> Array: ...      # 房间内可配置项（回合数、家规）
func setup(players: Array, cfg: Dictionary) -> void: ...
func start_round() -> void: ...
func on_player_input(peer_id: int, payload: PackedByteArray) -> void: ...
func is_finished() -> bool: ...
func get_results() -> Array: ...
func get_replay_data() -> Dictionary: ...   # 揭晓回放 / 分享长图
```

房间内切换游戏就只是 `unload(current) → load(next)`。

### 5.2 主机/客户端同构原则

**主机上跑的是完整游戏逻辑 + 一个玩家界面。** 这意味着：

- 所有游戏逻辑代码只有一份，主机和客户端都编译进去；
- 用 `multiplayer.is_server()` 分支决定"我是权威还是表现"；
- **不要**为主机单独写一套逻辑，否则必然出同步问题。

### 5.3 Godot RPC 的坑（务必注意）

RPC 依赖节点的 `NodePath` 在两端一致。**主机和客户端的节点树必须完全同构**，否则 RPC 静默失败——不报错，只是收不到，排查起来极其痛苦。

- 房间场景做成自包含的 `Room.tscn`，所有 RPC 挂在固定路径的节点上。
- CI 里加一条"双端节点路径一致性"的检查。

---

## 6. Godot 工程配置

| 项 | 建议 |
|---|---|
| 引擎版本 | **Godot 4.7 stable**（已安装在 `D:\Godot Progame\Godot_v4.7-stable_win64.exe`） |
| 语言 | GDScript 为主；C# 仅在需要复杂数值计算时局部引入（iOS 导出有额外复杂度） |
| 渲染器 | Mobile 渲染器 |
| 分辨率 | `stretch_mode = canvas_items`，`aspect = expand`，基准 1080×1920 竖屏 |
| 网络 | `ENetMultiplayerPeer` + `PacketPeerUDP`（局域网发现） |
| 后台保持 | Android 加 `WakeLock`；iOS 明确提示用户保持前台，切后台 ENet 连接会断 |

### 6.1 目录结构

```
res://
├── addons/
├── assets/                 # 图集、音频、字体
├── core/
│   ├── autoload/
│   │   ├── net.gd          # 连接状态机、重连、心跳
│   │   ├── discovery.gd    # UDP 广播发现 / 二维码 / 手动 IP
│   │   ├── session.gd      # 当前玩家、房间、配置
│   │   ├── audio.gd
│   │   └── profile.gd      # 本地档案（无账号）
│   ├── net/
│   │   ├── protocol.gd     # 报文定义、版本协商
│   │   ├── codec.gd        # 二进制编解码
│   │   └── qr.gd           # 二维码生成（房主）/ 解析
│   └── ui/                 # 通用控件
├── drawing/                # 绘制引擎（你画我猜 + 画猜接龙共用）
│   ├── board.tscn
│   ├── board.gd
│   ├── stroke_codec.gd
│   └── palette.gd
├── games/
│   ├── base_minigame.gd    # 统一接口
│   ├── draw_guess/
│   ├── telestrations/
│   └── uno/
│       ├── rules.gd        # 纯逻辑规则层
│       ├── deck.gd
│       └── ui/
└── data/                   # 词库 CSV、卡牌定义、配置 JSON
```

---

## 7. 商业化（局域网版）

局域网游戏没有在线服务，变现方式**和在线版完全不同**：

| 模式 | 说明 | 评价 |
|---|---|---|
| **免费下载 + 房主解锁（推荐）** | 所有人都能免费下载并加入房间；只有购买过的玩家能**当房主开局** | 参考 Jackbox。传播阻力最小，付费点落在"我就是想约人玩"这个明确意愿上 |
| 买断制（¥18~30） | 一次性购买，全部游戏解锁 | 简单，但下载转化率低 |
| 皮肤 / 词包内购 | 头像部件、画笔皮肤、出牌特效、节日词包 | 作为补充 |
| 广告 | 结算页插屏（严格限制频率） | 局域网游戏插广告体验很差，谨慎 |

**推荐组合：免费下载 + 房主解锁（¥25 左右）+ 皮肤内购。**

### 7.1 支付的技术注意

- 走 Apple IAP / Google Play Billing，**必须实现收据校验**。局域网版本没有自己的服务器，只能用本地校验或平台方的服务端校验接口——纯本地校验存在被破解风险，但对单机游戏是可接受的成本权衡，不建议为此专门搭服务器。
- 若需要更强的防破解，可接入第三方（如 RevenueCat）承担收据校验，仍无需自建服务。
- **Android 自定义插件**：IAP、广告、扫码都需要写 Android Plugin（v2）或 Java singleton，这是排期里要预留的工作量（约 1~2 周）。

---

## 8. 合规（大幅简化）

| 事项 | 局域网版的情况 |
|---|---|
| **游戏版号** | 国内渠道上架**仍然需要版号**。但无在线服务可按"单机游戏"路径申请，比网络游戏流程简单、周期更短 |
| ICP / 文网文 | **不需要**（无在线服务） |
| 实名认证 / 防沉迷 | **不需要**（非网络游戏） |
| 隐私政策 | **仍需要**。必须说明：访问本地网络（iOS 权限）、IAP、广告 SDK、本地存储 |
| 内容审核 | 玩家自定义昵称与画板内容不上传，**审核压力大幅降低**。仍建议保留本地敏感词过滤和举报入口 |

> 版号仍是排期里最不可控的一项。**建议第一周就去咨询**，同时准备海外版先行（Google Play / App Store 全球区）或 TestFlight / 内测分发验证玩法。

---

## 9. 局域网排障：这是产品功能，不是文档

局域网联机最大的敌人不是技术，是**环境**。必须把这些当成产品需求来做。

### 9.1 最常见的失败原因

| # | 原因 | 表现 | 对策 |
|---|---|---|---|
| 1 | **路由器开启客户端隔离（AP Isolation）** | 能连 Wi-Fi，但互相发现不到、连不上 | **主推热点模式**；失败时直接引导"请让房主开热点" |
| 2 | **iOS 本地网络权限被拒绝** | 连接超时，没有任何错误提示 | 首次进入即弹前置说明；失败时给"去设置开启本地网络"的引导 |
| 3 | 两人不在同一网段（一个 4G、一个 Wi-Fi） | 完全连不上 | 连接前检测本机 IP 是否有效，无效就直接提示 |
| 4 | 公共 Wi-Fi 需网页认证 | 显示已连接但实际不通 | 提示"这个 Wi-Fi 需要浏览器登录，建议改用手机热点" |
| 5 | 防火墙 / 端口被占用 | 建房失败 | 端口自动切换 + 明确错误码 |
| 6 | 房主切后台 / 来电 | 全员掉线 | 申请 WakeLock；提示保持前台；掉线后自动重连到主机 |

### 9.2 必须做的三个 UI

1. **房主建房页**：一张大二维码 + 房号 + "我的 IP" + 一行提示"连不上？试试开手机热点"。
2. **连接状态面板**：实时显示每个玩家的延迟（ping）和连接状态图标。**让问题可见，比事后猜有用得多。**
3. **三步排障引导**：连接失败时不要只弹"连接失败"，而是弹可操作的三步图：
   - ① 确认所有人在同一个 Wi-Fi（或都连了房主的热点）
   - ② iOS 用户检查"本地网络"权限是否开启
   - ③ 还不行就让房主开手机热点，其他人连上后重新扫码

> 经验值：**这一步做得好不好直接决定商店评分。** 局域网游戏的低分评论里，"连不上"几乎是第一大原因。

---

## 10. 里程碑排期（3~4 人全职团队）

| 里程碑 | 周期 | 交付物 | 验收标准 |
|---|---|---|---|
| **M0 局域网原型** | 1.5 周 | 3 台真机（含 iOS + Android）通过热点、扫码、广播发现三种方式进入同一房间 | **三种配对方式各成功 10/10 次**；iOS 权限流程跑通 |
| **M1 框架 + UNO** | 4 周 | 房间系统、玩家注册表、状态机、UNO 完整可玩（家规、重连、AI 托管、主机退出处理） | 8 人局连续跑完 3 局不掉线；房主退出有明确提示 |
| **M2 你画我猜** | 3.5 周 | 画板、笔刷、笔迹同步、词库系统、猜词交互、计分 | 8 人局笔迹同步顺滑；词库可分类开关 |
| **M3 画猜接龙** | 1.5 周 | 流水线调度、翻页动画、揭晓回放、长图分享 | 8 人链条完整跑通且能导出分享图 |
| **M4 排障与打磨** | 2.5 周 | 排障引导 UI、连接状态面板、美术替换、音效、新手引导、后台保活 | 20 名陌生用户首次连接成功率 ≥ 95% |
| **M5 商业化与上线** | 3 周 | IAP 插件、收据校验、隐私政策、版号材料、商店素材 | 双端提审通过 |

**合计约 16 周（4 个月）**，其中 M5 可与 M4 部分并行。

> 如果只有 1~2 人：先做 **UNO + 你画我猜**，**只做二维码配对**（砍掉广播发现），约 8~9 周可以出可玩版本。

---

## 11. 风险清单（局域网版）

| # | 风险 | 影响 | 优先级 | 对策 |
|---|---|---|---|---|
| **R1** | **路由器客户端隔离导致连不上** | 玩家根本无法开局 | **最高** | 主推热点模式；扫码直连；三步排障引导 |
| **R2** | **iOS 本地网络权限被拒** | 静默失败，极难排查 | **最高** | M0 就验证；前置说明 + 设置页引导 |
| R3 | 房主中途退出 / 切后台 / 来电 | 整局中断 | 高 | 对局中明确提示；大厅阶段做"成为新房主"；Android WakeLock |
| R4 | 8 人共连一个手机热点 | 发热、卡顿、掉线 | 中 | 压测热点模式；限制最大 8 人；降低广播频率 |
| R5 | RPC NodePath 不一致 | 玄学 bug | 中 | 场景自包含 + CI 一致性检查 |
| R6 | 手机后台被杀进程 | 掉线 | 中 | 保活提示 + 自动重连 |
| R7 | 安卓原生插件（IAP / 扫码 / 权限） | 排期外溢 | 中 | M0~M1 期间就做最小验证，不要等到上线前 |
| R8 | 版号周期不可控 | 无法上架国内渠道 | 中 | 尽早咨询；海外 / 内测先行 |
| R9 | 词库不够大导致重复率高 | 体验衰减 | 低 | 起步 900 词 + 支持导入自定义词包 |
| R10 | 局内时长过长导致疲劳 | 留存下降 | 低 | 单局 3~8 分钟；结算页给"再来一局 / 换游戏"一键入口 |

---

## 12. 上线后的验证指标

| 指标 | 目标 |
|---|---|
| **首次连接成功率** | **≥ 95%** |
| 房主从打开 App 到显示二维码 | < 15 s |
| 加入者从扫码到进房 | < 10 s |
| 局域网内延迟 | < 30 ms |
| 单会话平均局数 | ≥ 3 |
| 单局完成率 | ≥ 85% |
| 崩溃率 | < 0.5% 会话 |
| 商店评分 | ≥ 4.3，"连不上"类差评占比 < 10% |

如果**首次连接成功率 < 90%**：暂停加新游戏，全力修连接体验。这是局域网游戏的生死线。

---

## 13. 现在就开始做的 5 件事

1. **搭 3 台真机的局域网原型**（建议 1 台 iOS + 2 台 Android）：一个房间标题 + 二维码 + 玩家列表。**先把 iOS 本地网络权限和扫码直连跑通。**
2. **测试最恶劣的环境**：找一台开了客户端隔离的路由器，确认热点模式能救回来。
3. **把 UNO 规则定稿**并写进 `data/rules.yaml`（含所有家规开关）。规则文档要先于代码定稿。
4. **咨询版号与合规路径**，确认"无在线服务"是否可按单机游戏申报。
5. **搭起 CI 骨架**：Android 导出、iOS 导出、双端节点路径一致性检查。移动端手工打包会消耗你大量时间。

---

## 附 A：待你确认的问题

1. 主推场景到底是「**房主开热点**」还是「**同一个 Wi-Fi**」？这会改变 UI 的重心。
2. 是否接受「**房主退出就解散房间**」？还是要做主机迁移？
3. 要不要保留**单机热座模式**作为兜底？
4. 商业化走「**免费下载 + 房主解锁**」还是「买断制」？
5. 团队有几个人？是否有美术、以及能写 Android / iOS 原生插件的人？

## 附 B：如果将来要转在线模式

架构上留一个 `Transport` 抽象层（`connect_to_host()` / `send()` / 信号回调），把「ENet 直连」与「连到云端服务器」做成两个实现。这样将来加在线模式时，游戏逻辑层（UNO 规则、画猜调度）**一行都不用改**，只需：

1. 把主机权威逻辑搬到 Godot headless 服务器（同一份代码）；
2. 增加账号、匹配、房间列表服务；
3. 补上版号（网络游戏类别）、ICP、实名防沉迷。

**预留这个抽象层的成本约 2~3 天，建议现在就做。**
